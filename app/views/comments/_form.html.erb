<%= form_for([@article, @article.comments.build]) do |f| %>
	<%= f.label :author %><br>
	<%= f.text_field :author %><br>
	<br>
	<%= f.label :content %><br>
	<%= f.text_area :content %><br>
	<br>
	<%= f.submit %>
<% end %>
