<p id="notice"><%= notice %></p>
<h1>Home</h1>
<%= button_to "Admin", articles_path, :method => "get" %>

<table>
    <thead>
        <tr>
            <th>Headline</th>
            <th>Publication Date</th>
            <th colspan="3"></th>
        </tr>
    </thead>
    <tbody><% @articles.each do |article| %>
        <tr>
            <td><%= article.headline %></td>
            <td><%= article.created_at.strftime("%d %B %Y %-H:%M") %></td>
            <td><%= link_to 'Show', article %></td>
            <td>
        </tr>
        <% end %></tbody>
</table>
<br>
